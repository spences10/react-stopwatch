{
  "name": "react-stopwatch",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "16.6.0",
    "react-dom": "16.6.0",
    "react-scripts": "2.0.5",
    "styled-components": "4.0.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "npm run clean && react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "clean": "rm -rf stopwatch/ && rm -rf build/",
    "format": "pretty-quick",
    "precommit": "pretty-quick --staged"
  },
  "pre-commit": [
    "format"
  ],
  "devDependencies": {
    "prettier": "1.14.3",
    "pretty-quick": "1.8.0"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
